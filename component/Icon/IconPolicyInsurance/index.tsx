import React from "react";
import { Entypo } from "@expo/vector-icons";
import { IconType } from "../TypeIcon";

const IconPolicyInsurance = ({ size = 24, color = "#2febbc" }: IconType) => {
  return <Entypo name="text-document" size={size} color={color} />;
};

export default IconPolicyInsurance;
